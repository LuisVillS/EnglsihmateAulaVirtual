-- Active: 1696069830194@@127.0.0.1@3306@discord_bot
CREATE DATABASE discord_bot;
mysql -u root -p

SHOW FULL TABLES

VIEW users 

USE users;

SHOW TABLES 

USE discord_bot;

SELECT * FROM users 

UPDATE users SET DiscordID = NULL WHERE Correo = 'ejemplo3@gmail.com';

DROP TABLE IF EXISTS preguntas_banquea;

CREATE TABLE preguntas_banquea (
    id INT AUTO_INCREMENT PRIMARY KEY,
    tema VARCHAR(255),
    pregunta TEXT,
    respuesta_correcta VARCHAR(255),
    respuesta_incorrecta1 VARCHAR(255),
    respuesta_incorrecta2 VARCHAR(255),
    respuesta_incorrecta3 VARCHAR(255),
    razon TEXT,
    UNIQUE KEY (pregunta(255))
);



SELECT * FROM preguntas_banquea

ALTER TABLE preguntas_banquea MODIFY COLUMN pregunta TEXT;


CREATE TABLE puntaje_banquea (
    user_id BIGINT,
    respuestas_correctas INT DEFAULT 0,
    respuestas_incorrectas INT DEFAULT 0,
    veces_jugadas INT DEFAULT 0,
    puntaje_promedio FLOAT DEFAULT 0.0,
    PRIMARY KEY (user_id)
);

SELECT * FROM puntaje_banquea


DELETE FROM preguntas_banquea;
